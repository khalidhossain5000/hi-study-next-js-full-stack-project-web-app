import FreeEnrolledCard from '@/components/Dashboard/Admin/FreeEnrolled/FreeEnrolledCard';
import React from 'react';

const FreeEnrolledInfo = () => {
    return (
        <div>
            <FreeEnrolledCard/>
        </div>
    );
};

export default FreeEnrolledInfo;